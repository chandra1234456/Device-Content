<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Shown by the OS via androidx.core.splashscreen while the Activity/Compose content is cold-starting. -->
    <style name="Theme.DeviceInfo.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo_animated</item>
        <item name="windowSplashScreenAnimationDuration">450</item>
        <item name="postSplashScreenTheme">@style/Theme.DeviceInfo</item>
    </style>

    <!-- Base application theme. All real Material 3 theming (colors, typography, shapes) lives in
         ui/theme/Theme.kt; this only needs to exist so the window itself (status/nav bar, splash
         hand-off) isn't left on the platform default theme before Compose takes over. -->
    <style name="Theme.DeviceInfo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>
